<Window x:Class="NetworkDiagramApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetworkDiagramApp"
        mc:Ignorable="d"
        Title="ネットワーク図作成ツール v1.0.2" 
        Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="#F8F8F8">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:CountToEnabledConverter x:Key="CountToEnabledConverter"/>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="ネットワーク図作成ツール" 
                       Style="{StaticResource TitleText}"/>
            <TextBlock Text="ExcelデータからDraw.io形式の構成図を自動生成" 
                       Style="{StaticResource SubtitleText}"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Excelファイル選択 -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8" 
                Padding="20"
                Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" 
                                  ShadowDepth="2" 
                                  Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                           Text="📁 入力ファイル" 
                           FontWeight="SemiBold"
                           FontSize="14"
                           Margin="0,0,0,10"/>

                <TextBox Grid.Row="1" Grid.Column="0"
                         x:Name="TxtExcelPath"
                         IsReadOnly="True"
                         Margin="0,0,10,0"
                         VerticalContentAlignment="Center"
                         Text="{Binding ExcelPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Excelファイルを選択してください..." 
                                                               Foreground="#AAAAAA" 
                                                               Margin="8,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Grid.Row="1" Grid.Column="1"
                        Content="📂 参照"
                        Style="{StaticResource SecondaryButton}"
                        Click="BtnSelectExcel_Click"/>

                <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                           Text="📄 読み込むシート" 
                           FontWeight="SemiBold"
                           FontSize="13"
                           Margin="0,15,0,8"/>

                <ComboBox Grid.Row="3" Grid.ColumnSpan="2"
                          ItemsSource="{Binding AvailableSheets}"
                          SelectedItem="{Binding SelectedSheet}"
                          FontSize="13"
                          Padding="8"
                          IsEnabled="{Binding AvailableSheets.Count, Converter={StaticResource CountToEnabledConverter}}"/>
            </Grid>
        </Border>

        <!-- 出力ファイル名 -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="8" 
                Padding="20"
                Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" 
                                  ShadowDepth="2" 
                                  Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="💾 出力ファイル名（省略時: network.drawio）" 
                           FontWeight="SemiBold"
                           FontSize="14"
                           Margin="0,0,0,10"/>

                <TextBox Grid.Row="1"
                         x:Name="TxtOutputName"
                         VerticalContentAlignment="Center"
                         Text="{Binding OutputName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="network.drawio" 
                                                               Foreground="#AAAAAA" 
                                                               Margin="8,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Grid>
        </Border>

        <!-- 生成ボタン -->
        <Button Grid.Row="3"
                Content="🚀 図を生成"
                Style="{StaticResource PrimaryButton}"
                Height="50"
                Margin="0,0,0,20"
                Click="BtnGenerate_Click"
                IsEnabled="{Binding CanGenerate}"/>

        <!-- プログレスバー -->
        <Grid Grid.Row="4" Margin="0,0,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ProgressBar Grid.Row="0"
                         x:Name="ProgressBar"
                         Height="8"
                         IsIndeterminate="{Binding IsProcessing}"
                         Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <TextBlock Grid.Row="1"
                       x:Name="LblStatus"
                       Text="{Binding StatusText}"
                       Foreground="#666666"
                       FontSize="12"
                       Margin="0,5,0,0"
                       HorizontalAlignment="Center"/>
        </Grid>

        <!-- ログエリア -->
        <Border Grid.Row="5"
                Background="White" 
                CornerRadius="8"
                Padding="0"
                Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" 
                                  ShadowDepth="2" 
                                  Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" 
                        Background="#F3F3F3" 
                        CornerRadius="8,8,0,0"
                        Padding="15,10">
                    <TextBlock Text="📋 処理ログ" 
                               FontWeight="SemiBold"
                               FontSize="13"/>
                </Border>

                <ScrollViewer Grid.Row="1" 
                              VerticalScrollBarVisibility="Auto"
                              Background="#1E1E1E">
                    <TextBlock x:Name="TxtLog"
                               FontFamily="Consolas"
                               FontSize="11"
                               Foreground="#00FF00"
                               Padding="15"
                               TextWrapping="Wrap"
                               Text="{Binding LogText}"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- フッター -->
        <TextBlock Grid.Row="7"
                   Text="© 2025 Network Diagram Generator | Version 1.0"
                   Foreground="#AAAAAA"
                   FontSize="11"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,0"/>
    </Grid>
</Window>
